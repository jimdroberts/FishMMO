{
  "ConnectionStrings": {
    "NpgsqlConnection": ""
  },
  // !!! WARNING: HmacKey MUST be set via environment variable FISHMMO_CONNECTION_TOKEN_HMAC_KEY_BASE64 in production. !!!
  // An empty value will cause connection token verification to fail (or silently use a weak empty key).
  // Generate: openssl rand -base64 32
  "ConnectionToken": {
    "HmacKey": ""
  },
  "WebServer": {
    "HttpPort": 8080
  }
}
