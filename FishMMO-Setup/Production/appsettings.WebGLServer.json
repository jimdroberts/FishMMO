{
  "_comment": "Production configuration for FishMMO WebGL Server. Overrides Development defaults. All secrets (client gate secret) MUST be set via environment variables. Do not commit real credentials here.",
  "WebServer": {
    "HttpPort": 8000
  }
}
