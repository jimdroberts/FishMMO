{
  "_comment": "Production configuration for FishMMO Discord Bot. Overrides Development defaults. The bot token and database connection string MUST be set via environment variables (Discord__Token, ConnectionStrings__Npgsql). Do not commit real credentials here.",
  "Discord": {
    "Token": "",
    "_comment_token": "Token MUST be set via environment variable Discord__Token at runtime. An empty token causes the bot to fail at startup. Never commit the token to source control.",
    "DefaultGuildId": 0,
    "_comment_guild_id": "0 means disabled. Set to a real Discord guild ID (snowflake) for the bot to operate in a specific server."
  },
  "ConnectionStrings": {
    "Npgsql": "Host=localhost;Port=5432;Database=fishmmo;Username=;Password=;"
  },
  "ChatPollingIntervalSeconds": 5,
  "_comment_chat_relay": "ChatRelay.GameToDiscordChannels is an ALLOWLIST of in-game chat channels the bot may republish to Discord. Omit it to use the built-in default (Say, World, Trade, Region) — public channels only. Tell, Guild and Party are private and are refused even if named here; making them relayable is a code change, not a config change.",
  "ChatRelay": {
    "GameToDiscordChannels": [ "Say", "World", "Trade", "Region" ]
  },
  "_comment_bridge_length": "BridgeMessageMaxLength caps a Discord message bridged into the game. Keep it at or below the game's ChatBroadcast.MaxTextLength (128) — clients discard anything longer, so a larger value makes long messages vanish rather than arrive truncated. This was 2000, so every bridged message over 128 characters was silently dropped by every client.",
  "BridgeMessageMaxLength": 128,
  "RateLimiting": {
    "MaxMessagesPerWindow": 10,
    "WindowSeconds": 60
  },
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning",
      "Discord": "Warning",
      "FishMMO.DiscordBot": "Information",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  }
}
