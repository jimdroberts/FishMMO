{
  "_comment": "Non-sensitive defaults only. Override via Npgsql__* env vars. Leave Database empty in templates; set fish_mmo_postgresql (prod) or local name via secrets — never overwrite operator prod cfg with a wrong hardcode.",
  "Npgsql": {
    "Database": "",
    "Username": "",
    "Password": "",
    "Host": "127.0.0.1",
    "Port": "5432"
  }
}
