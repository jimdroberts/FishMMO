{
  "_comment": "Non-sensitive defaults only. Override secrets via environment variables: Discord__Token, ConnectionStrings__Npgsql (see fishmmo-secrets.env or fish shell snippet).",
  "Discord": {
    "Token": "",
    "_comment_token": "Token MUST be set via environment variable Discord__Token at runtime. An empty token causes the bot to fail at startup. Never commit the token to source control.",
    "DefaultGuildId": 0,
    "_comment_guild_id": "0 means disabled. Set to a real Discord guild ID (snowflake) for the bot to operate in a specific server."
  },
  "ConnectionStrings": {
    "Npgsql": "Host=localhost;Port=5432;Database=fishmmo;Username=;Password=;"
  },
  "_comment2": "ChatPollingIntervalSeconds polls the SQL database for new messages. Set to 5 for development to reduce database load; tune for production based on expected chat volume. Consider 1-2s for production.",
  "ChatPollingIntervalSeconds": 5,
  "BridgeMessageMaxLength": 500,
  "RateLimiting": {
    "MaxMessagesPerWindow": 5,
    "WindowSeconds": 10
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Discord": "Debug",
      "FishMMO.DiscordBot": "Debug",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  }
}
