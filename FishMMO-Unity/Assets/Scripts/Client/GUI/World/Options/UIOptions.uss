/*
    UIOptions.uss
    Layout for the settings panel.
*/

.options-root {
    flex-grow: 1;
    align-items: center;
    justify-content: center;
    background-color: var(--overlay-dim);
}

.options-panel {
    min-width: 420px;
    max-width: 560px;
    max-height: 80%;
    padding: 16px;
}

.options-title {
    font-size: 20px;
    -unity-font-style: bold;
    margin-bottom: 12px;
    -unity-text-align: middle-center;
}

.options-scroll {
    flex-grow: 1;
    margin-bottom: 12px;
}

.options-row {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    margin: 4px 0;
    min-height: 28px;
}

.options-row-label {
    flex-shrink: 0;
    width: 140px;
}

.options-row-field {
    flex-grow: 1;
    margin-left: 12px;
}

.options-footer {
    flex-direction: row;
    justify-content: flex-end;
}

.options-action {
    min-width: 96px;
}

/* ═══════════════════════════════════════════════════════════
   HEADER
   ═══════════════════════════════════════════════════════════ */

.options-header {
    flex-direction: row;
    align-items: center;
    height: 36px;
    padding-left: 12px;
    padding-right: 12px;
    flex-shrink: 0;
}

.options-header__icon {
    width: 16px;
    height: 16px;
    margin-right: 8px;
    flex-shrink: 0;
}

/* ═══════════════════════════════════════════════════════════
   SECTIONS
   Screen / Gameplay / Colours.
   ═══════════════════════════════════════════════════════════ */

.options-section {
    margin-top: 14px;
    margin-bottom: 4px;
    padding-bottom: 3px;
    flex-shrink: 0;
}

/*
    Container for the rows UITKOptions generates. It has no size of its own — the rows drive
    the height — but flex-shrink: 0 stops the ScrollView compressing the list instead of
    scrolling it once the three sections together exceed the panel's max height.
*/
.options-list {
    flex-direction: column;
    flex-shrink: 0;
}

.options-row--action {
    justify-content: flex-end;
}

.options-reset-btn {
    height: 26px;
    padding-left: 12px;
    padding-right: 12px;
    flex-shrink: 0;
}

/* ═══════════════════════════════════════════════════════════
   COLOUR SWATCHES
   ═══════════════════════════════════════════════════════════ */

/*
    The swatch previews the colour a row controls. Its background is written inline by
    UITKOptions when an override exists; when none does the --unset modifier below supplies
    the appearance instead, so "no choice made" is visibly different from "chose white".
*/
.options-swatch {
    width: 34px;
    height: 18px;
    border-radius: 3px;
    border-width: 1px;
    border-color: var(--abyss-400);
    margin-right: 8px;
    flex-shrink: 0;
}

.options-swatch--unset {
    background-color: var(--abyss-700);
    border-color: var(--abyss-500);
}

.options-swatch-btn {
    width: 74px;
    height: 22px;
    flex-shrink: 0;
}

/* ═══════════════════════════════════════════════════════════
   SCREEN SECTION — staged apply
   Apply arms an auto-revert countdown; Keep disarms it. See UITKOptions.
   ═══════════════════════════════════════════════════════════ */

.options-screen-status {
    flex-grow: 1;
    font-size: 10px;
    -unity-text-align: middle-left;
    overflow: hidden;
}

.options-screen-btn {
    height: 24px;
    min-width: 68px;
    margin-left: 4px;
    flex-shrink: 0;
}

/* ═══════════════════════════════════════════════════════════
   CONTROLS SECTION
   ═══════════════════════════════════════════════════════════ */

/*
    The binding button is the control itself: pressing it starts listening, and its own label
    becomes the prompt. That keeps a row to two controls instead of three and makes it obvious
    which binding is being captured, because only one row can ever be in the listening state.
*/
.options-bind-btn {
    width: 130px;
    height: 22px;
    font-size: 11px;
    flex-shrink: 0;
}

.options-bind-btn--listening {
    background-color: var(--brand-mid);
    border-color: var(--brand-border);
    color: var(--text-bright);
}

/*
    A binding that collides with another action. Left applied until the conflict is resolved, so
    scrolling away and back does not lose the warning.
*/
.options-bind-btn--conflict {
    background-color: var(--danger-tint);
    border-color: var(--danger-border);
}

.options-bind-reset {
    width: 26px;
    height: 22px;
    margin-left: 4px;
    font-size: 11px;
    flex-shrink: 0;
}

.options-controls-status {
    margin-top: 2px;
    margin-bottom: 2px;
    min-height: 13px;
    font-size: 10px;
    flex-shrink: 0;
}
