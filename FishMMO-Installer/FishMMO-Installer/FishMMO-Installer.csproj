<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>FishMMO.Installer</RootNamespace>
    <AssemblyName>FishMMO-Installer</AssemblyName>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <NoWarn>$(NoWarn);MSB3277</NoWarn>

    <!-- Source Paths (evaluated in static phase; MSBuildProjectDirectory is always available). -->
    <FishMMODatabaseSource>$([System.IO.Path]::GetFullPath('$(MSBuildProjectDirectory)/../../FishMMO-Database'))</FishMMODatabaseSource>
    <FishMMOSharedUtilitySource>$([System.IO.Path]::GetFullPath('$(MSBuildProjectDirectory)/../../FishMMO-SharedUtility'))</FishMMOSharedUtilitySource>

    <!-- Stale-copy sentinel paths (project-dir subdirs written by old broken builds). -->
    <FishMMODatabaseStale>$(MSBuildProjectDirectory)/FishMMO-Database</FishMMODatabaseStale>
    <FishMMOSharedUtilityStale>$(MSBuildProjectDirectory)/FishMMO-SharedUtility</FishMMOSharedUtilityStale>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\FishMMO-Database\FishMMO-DB\FishMMO-DB.csproj" />
    <ProjectReference Include="..\..\FishMMO-Logger\FishMMO-Logger\FishMMO-Logger.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Configuration.Binder" Version="9.0.4" />
  </ItemGroup>

  <ItemGroup>
    <None Update="checksums.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <!-- Shared logging configuration from FishMMO-Setup -->
    <None Include="..\..\FishMMO-Setup\logging.json">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <Link>logging.json</Link>
    </None>
  </ItemGroup>

  <!-- Clean previously copied trees (from previous builds) -->
  <Target Name="CleanCopiedSourceTrees" BeforeTargets="Build">
    <!-- Dest is defined here so $(OutDir) is fully resolved at execution time. -->
    <PropertyGroup>
      <FishMMODatabaseDest>$(OutDir)FishMMO-Database</FishMMODatabaseDest>
      <FishMMOSharedUtilityDest>$(OutDir)FishMMO-SharedUtility</FishMMOSharedUtilityDest>
    </PropertyGroup>
    <!-- Remove output-dir copies (incremental clean). -->
    <RemoveDir Directories="$(FishMMODatabaseDest)" Condition="Exists('$(FishMMODatabaseDest)')" />
    <RemoveDir Directories="$(FishMMOSharedUtilityDest)" Condition="Exists('$(FishMMOSharedUtilityDest)')" />
    <!-- Remove any stale copies left in the project directory by old broken builds. -->
    <RemoveDir Directories="$(FishMMODatabaseStale)" Condition="Exists('$(FishMMODatabaseStale)')" />
    <RemoveDir Directories="$(FishMMOSharedUtilityStale)" Condition="Exists('$(FishMMOSharedUtilityStale)')" />
  </Target>

  <!-- Copy FishMMO-Database -->
  <!-- Run after the project build completes so $(OutDir) points to the real build output. -->
  <Target Name="CopyFishMMODatabase" AfterTargets="Build">
    <PropertyGroup>
      <FishMMODatabaseDest>$(OutDir)FishMMO-Database</FishMMODatabaseDest>
    </PropertyGroup>
    <ItemGroup>
      <FishMMODatabaseFiles Include="$(FishMMODatabaseSource)\**\*.*"
        Exclude="
          $(FishMMODatabaseSource)\**\bin\**;
          $(FishMMODatabaseSource)\**\obj\**;
          $(FishMMODatabaseSource)\.vs\**" />
    </ItemGroup>

    <Copy
      SourceFiles="@(FishMMODatabaseFiles)"
      DestinationFiles="@(FishMMODatabaseFiles->'$(FishMMODatabaseDest)\%(RecursiveDir)%(Filename)%(Extension)')"
      SkipUnchangedFiles="true" />

    <Message Text="Copied FishMMO-Database to $(FishMMODatabaseDest)" Importance="high" />
  </Target>

  <!-- Copy-and-rename config files from FishMMO-Setup/Development/ to output. -->
  <Target Name="CopyFishMMOConfig" AfterTargets="Build">
    <!-- Installer's own appsettings.json (Npgsql database config). -->
    <Copy SourceFiles="..\..\FishMMO-Setup\Development\appsettings.Database.json"
          DestinationFiles="$(OutDir)appsettings.json"
          SkipUnchangedFiles="true" />
    <!-- Installer templates. -->
    <MakeDir Directories="$(OutDir)templates" />
    <Copy SourceFiles="..\..\FishMMO-Setup\Development\install-config.quickstart.json"
          DestinationFiles="$(OutDir)templates\install-config.quickstart.json"
          SkipUnchangedFiles="true" />
    <Copy SourceFiles="..\..\FishMMO-Setup\Development\install-config.full.json"
          DestinationFiles="$(OutDir)templates\install-config.full.json"
          SkipUnchangedFiles="true" />
    <Copy SourceFiles="..\..\FishMMO-Setup\Development\install-config.web.json"
          DestinationFiles="$(OutDir)templates\install-config.web.json"
          SkipUnchangedFiles="true" />
  </Target>

  <!-- Copy FishMMO-SharedUtility -->
  <!-- Run after the database copy finishes (which runs after Build). -->
  <Target Name="CopyFishMMOSharedUtility" AfterTargets="CopyFishMMODatabase">
    <PropertyGroup>
      <FishMMOSharedUtilityDest>$(OutDir)FishMMO-SharedUtility</FishMMOSharedUtilityDest>
    </PropertyGroup>
    <ItemGroup>
      <FishMMOSharedUtilityFiles Include="$(FishMMOSharedUtilitySource)\**\*.*"
        Exclude="
          $(FishMMOSharedUtilitySource)\**\bin\**;
          $(FishMMOSharedUtilitySource)\**\obj\**;
          $(FishMMOSharedUtilitySource)\.vs\**" />
    </ItemGroup>

    <Copy
      SourceFiles="@(FishMMOSharedUtilityFiles)"
      DestinationFiles="@(FishMMOSharedUtilityFiles->'$(FishMMOSharedUtilityDest)\%(RecursiveDir)%(Filename)%(Extension)')"
      SkipUnchangedFiles="true" />

    <Message Text="Copied FishMMO-SharedUtility to $(FishMMOSharedUtilityDest)" Importance="high" />
  </Target>

</Project>